---
import { services } from "@/lib/constants";
import Image from "astro/components/Image.astro";
---

<section class="py-16 px-4 sm:px-6 lg:px-20">
  <div class="text-center max-w-3xl mx-auto">
    <h2 class="text-3xl font-bold sm:text-4xl">
      Generate AI Copy Writing Favorite Tools
    </h2>
    <p class="mt-4 text-gray-600">
      The discovery was made by Richard McClintock, a professor of Latin at
      Hampden-Sydney College in Virginia, who faced the
    </p>
  </div>

  <div class="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3 ">
    {
      services.map((service) => (
        <div class="border rounded-lg p-6 shadow-sm bg-white group flex flex-col hover:border-main transition-all duration-200 cursor-pointer">
          <Image 
            src={service.image}
            alt={service.title}
            class="w-3/4 h-auto mb-auto"
          />
          <h3 class="text-xl font-semibold text-gray-900 my-auto">{service.title}</h3>
          <p class="text-gray-600 text-sm ">{service.description}</p>
          <a
            href={service.link}
            class="mt-4 inline-block text-black group-hover:text-main text-sm font-medium transition-all duration-200"
          >
            {service.linkText} →
          </a>
        </div>
      ))
    }
  </div>
</section>
