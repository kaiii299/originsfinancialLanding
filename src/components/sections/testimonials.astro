---
import { testimonials } from "@/lib/constants";
import { LuQuote } from "react-icons/lu";
import TitleComponent from "@/components/ui/title.astro";
---

<section class="">
    <div class="text-center max-w-3xl mx-auto">
      <TitleComponent title={'Comapany Received 4.8/5 Stars in Over 10,000+ Reviews'} />
      <p class="mt-4 text-gray-600">
        The discovery was made by Richard McClintock, a professor of Latin at Hampden-Sydney College in Virginia, who faced the
      </p>
    </div>
  
    <div class="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 max-w-7xl mx-auto">
      {testimonials.map((testimonial) => (
        <div class="p-6 border flex flex-col rounded-lg bg-white shadow-sm hover:shadow-md transition">
            <LuQuote className="text-main text-5xl"/>
          <p class="mt-4 text-gray-700 mb-2">{testimonial.quote}</p>
          <div class="mt-auto">
            <p class="text-lg font-medium text-gray-900">{testimonial.name}</p>
            <p class="text-sm text-gray-600">{testimonial.role}</p>
          </div>
        </div>
      ))}
    </div>
  </section>