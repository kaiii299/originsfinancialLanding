---
import Image from "astro/components/Image.astro";
import { Button } from "./ui/button";
import ScrollingLogo from "./ui/scrolling-logo.astro";
import { heroSection } from "@/lib/constants";
---

<section class="bg-white py-16 px-4 sm:px-6 lg:px-20">
  <div
    class="max-w-7xl mx-auto flex flex-col lg:flex-row items-center justify-between"
  >
    <!-- Left Content -->
    <div class="max-w-lg text-start lg:text-left">
      <h1 class="font-bold text-3xl md:text-5xl leading-tight text-gray-900">
        {heroSection.title}
      </h1>
      <p class="mt-6 text-gray-600">
        {heroSection.subtitle}
      </p>
      <div class="mt-8 flex justify-start lg:justify-start space-x-4">
        <Button className="bg-yellow-400 text-black  hover:bg-main transition">
          Start For Free
        </Button>
        <Button variant={"outline"} className=" hover:bg-gray-100 transition">
          Browse Feature
        </Button>
      </div>
    </div>

    <!-- Right Image -->
    <div class="mt-10 lg:mt-0 lg:w-full">
      <Image
        src={heroSection.image}
        title={"Hero"}
        draggable="false"
        alt="AI Writing Tool Illustration"
        class="w-full"
      />
    </div>
  </div>

  <div class="mt-10">
    <ScrollingLogo />
  </div>
</section>
